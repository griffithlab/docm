.form-group
  = label_tag(id, label_text)
  = select_tag(id, options_for_select(collection, selected), multiple: true, class: ['chosen-select', 'form-control', 'filter-group'], data: {placeholder: 'All'} )
